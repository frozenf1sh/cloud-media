
apiVersion: v1
kind: Service
metadata:
  name: loki-service
  namespace: cloud-media
spec:
  selector:
    app: loki
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 3100
      targetPort: http
