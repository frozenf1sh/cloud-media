
apiVersion: v1
kind: Service
metadata:
  name: api-server-service
  namespace: cloud-media
spec:
  selector:
    app: api-server
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: http
