
apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: cloud-media
spec:
  selector:
    app: postgres
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 5432
      targetPort: tcp-postgresql
